import { NepaliNumberFormatterPipe } from './nepali-number-formatter.pipe';

describe('NepaliNumberFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new NepaliNumberFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
